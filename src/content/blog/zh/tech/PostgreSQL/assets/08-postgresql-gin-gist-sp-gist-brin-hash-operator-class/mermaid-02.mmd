flowchart TD
    Q[明确 SQL 谓词与排序] --> B{普通标量等值/范围/排序?}
    B -- 是 --> BT[B-tree 为基线]
    BT --> H{单列、仅等值、长键且无需唯一/排序?}
    H -- 是 --> HT[把 Hash 加入对照实验]
    H -- 否 --> END1[保留 B-tree]

    B -- 否 --> E{查询复合值中的元素/词项/三元组?}
    E -- 是 --> G[GIN]
    G --> J{JSONB?}
    J -- 是 --> JO{需要键存在类操作符吗}
    JO -- 是 --> OPS[jsonb_ops]
    JO -- 否 --> PATH[对比 jsonb_path_ops]

    E -- 否 --> S{范围/空间/网络/最近邻?}
    S -- 是 --> K{包围摘要还是明确空间分区?}
    K -- 包围/扩展通用 --> GI[GiST]
    K -- 分区树/前缀/点 --> SP[SP-GiST]
    GI --> NN{需要 KNN 或排除约束?}
    SP --> NN

    S -- 否 --> R{超大表且列值与 Heap 物理顺序相关?}
    R -- 是 --> BR[BRIN：比较摘要类型与 pages_per_range]
    R -- 否 --> DM[重新审视数据模型、表达式或分区]
