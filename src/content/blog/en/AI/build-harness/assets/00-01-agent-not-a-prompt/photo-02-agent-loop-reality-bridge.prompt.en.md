1. Diagram Type
Circular loop diagram, because the core idea is that the model keeps advancing the task each round based on new observations.

2. Visual Element List
The center is the Agent control core. Around it are 6 nodes: Current Field, Model Judgment, Action Intent, Tool Execution, Observation, State Update. Highlight Tool Execution and State Update. Use arrows to form a closed loop, and draw an external project folder on the right as the real world.

3. Positive Image Prompt
Create an in-article technical explanation image about "how an Agent loop turns a one-shot answer into continuous progress."

Style: off-white paper background, black hand-drawn marker linework, slightly uneven line weight, small amount of pale-yellow highlighting, editorial technical illustration, hand-drawn flow diagram for a technical blog, clear, restrained, with an engineering sketch feel.

Composition: circular flow diagram. In the center, draw a simple Agent control core icon, like a small control dial or circular gear. Around it, arrange 6 nodes clockwise: 1. Field: folder and small magnifying glass, representing the current task context. 2. Judgment: brain-like line drawing or model chip, representing the model deciding the next step. 3. Intent: small note and arrow, representing tool call or action proposal. 4. Execution: wrench and terminal window, representing the tool actually executing; highlight with pale yellow. 5. Observation: log scroll, representing tool output. 6. Update: stacked state cards, representing writing the result back to state; highlight with pale yellow. Add a faint project folder on the right to represent the real world being accessed outside the loop.

The background has very faint circuit lines and engineering sketch guidelines. Keep node labels short and avoid long sentences. Leave a blank handwritten quote area at the bottom. The image should look like a hand-drawn mechanism diagram in a technical blog.

4. Negative Prompt
No photorealism, no 3D, no complex UI screenshots, no large amounts of code, no dense tiny text, no complex tables, no cyberpunk, no neon colors, no dark background, no colorful cartoons, no complex shadows, no excessive decoration, no more than 6 main nodes, do not generate long text.
