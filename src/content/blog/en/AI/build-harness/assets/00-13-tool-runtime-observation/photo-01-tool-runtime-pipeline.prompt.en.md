1. Diagram Type
Horizontal pipeline, because the article's core is the controlled execution chain from the model's tool intent to the Runtime's observation.

2. Visual Element Checklist
Eight main nodes: Intent, Registry, Validate, Permission, Scheduler, Sandbox, Normalize, Observation. Intent uses a request form icon; Registry uses catalog cards; Validate uses a magnifier and checklist; Permission uses a shield gate; Scheduler uses a queue; Sandbox uses a boundary box; Normalize uses a funnel; Observation uses log paper and a loopback arrow. Highlight Permission and Observation in pale yellow. Draw a small curved arrow returning to the model on the right. Leave a bottom caption area.

3. Positive Image Prompt
Create an in-article technical explanation diagram about "Tool Runtime governing the model's tool intent into observation".

Style: off-white paper background, black hand-drawn pen linework with slightly uneven stroke width, a small amount of pale yellow highlight, editorial technical illustration, hand-drawn technical blog flowchart, clear, restrained, with an engineering sketch feel.

Composition: horizontal pipeline with eight key nodes from left to right connected by hand-drawn arrows. 1. Intent: request form icon, representing the model's tool request. 2. Registry: tool catalog card. 3. Validate: magnifier and checklist, representing schema and runtime validation. 4. Permission: shield gate, highlighted in pale yellow, representing allow / ask / deny. 5. Scheduler: small queue and clock, representing serial, concurrent, and timeout. 6. Sandbox: boundary box and working directory. 7. Normalize: funnel and organized card. 8. Observation: log paper and loopback arrow, highlighted in pale yellow, representing factual projection back to the next model round.

Use faint circuit traces and engineering sketch guides in the background without overpowering the main elements. Keep labels short. On the right, draw a curved arrow from Observation back to a small Model node. Leave one blank caption line at the bottom. Overall, it should look like a hand-drawn mechanism diagram in a technical blog, not a formal enterprise architecture chart.

4. Negative Prompt
No photorealism, no 3D, no complex UI screenshots, no large code blocks, no dense tiny text, no complex tables, no cyberpunk, no neon colors, no dark background, no colorful cartoon style, no complex shadows, no excessive decoration, no more than 8 main nodes, do not make background circuit lines more prominent than the subject, do not generate long text.
