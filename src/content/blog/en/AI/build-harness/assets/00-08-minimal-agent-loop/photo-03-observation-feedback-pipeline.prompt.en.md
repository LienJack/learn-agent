1. Figure Type

horizontal pipeline, because this image shows how raw tool results are organized, budgeted, summarized, and then routed into both model context and the event log.

2. Visual Element List

From left to right, 6 nodes: Raw Result, Normalize, Budget, Observation, Prompt Context, Event Log. Highlight Observation and Prompt Context. Raw Result uses a terminal log sheet, Event Log uses a ledger, and Prompt Context uses a projection beam.

3. Positive Image Prompt

Draw an in-article technical explanation image about how a tool result becomes an observation usable by the next model round.

Style: warm off-white paper background, black hand-drawn marker line art, slightly uneven line weight, a small amount of pale yellow highlighting, editorial technical illustration, hand-drawn flowchart for a technical blog, clear, restrained, with an engineering sketch feeling.

Composition: left-to-right horizontal pipeline. Include 6 key nodes connected by hand-drawn arrows: 1. Raw Result: terminal log sheet + failing-test X mark, representing raw stdout, file content, or an error. 2. Normalize: funnel + field card, representing structured organization. 3. Budget: scissors + gauge, representing truncating large results. 4. Observation: summary card + eye, highlighted in pale yellow, representing the summary and key evidence. 5. Prompt Context: projection beam + model chip, highlighted in pale yellow, representing what the next model round can see. 6. Event Log: ledger + timestamp, representing the system fact record.

Highlight: use pale yellow to emphasize Observation and Prompt Context, plus a thin arrow from Observation into Event Log.

Background: very faint circuit lines, node links, and engineering sketch guide lines; they must not compete with the main subject.

Text: Use short English-only visible labels. Leave a handwritten-style pull quote area at the bottom with the English quote: "A tool result is not a log; it is the next round's facts." Use English-only visible text.

The overall feeling should be like a hand-drawn mechanism diagram in a technical blog, not a product UI and not a formal architecture diagram. Elements should be clear, hierarchy should be clear, and arrow relationships should be unambiguous.

4. Negative Prompt

No photorealism, no 3D, no complex UI screenshots, no large blocks of code, no dense tiny text, no complex tables, no cyberpunk, no neon colors, no dark background, no colorful cartoon style, no complex shadows, no excessive decoration, no more than 8 main nodes, no background circuit lines more prominent than the main subject, no long text, no non-English visible text.
