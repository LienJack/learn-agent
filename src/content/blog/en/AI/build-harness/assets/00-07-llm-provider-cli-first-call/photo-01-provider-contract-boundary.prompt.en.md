1. Figure Type
Layered architecture diagram, because this image explains the responsibility boundaries between the CLI, Runtime, Provider Contract, Provider Adapter, and real Model API.

2. Visual Element List
Five main nodes: CLI, Runtime, Provider Contract, Provider Adapter, Model API. Highlight Provider Contract in pale yellow to show that it is the load-bearing layer that isolates provider details. Place a small translator icon near Provider Adapter and a cloud interface icon near Model API. The background may include very faint engineering circuit lines.

3. Positive Image Prompt
Draw an in-article technical explainer image about "the Provider Contract boundary in the first model call."

Style: off-white paper background, black hand-drawn marker line art, slightly uneven line weight, sparse pale-yellow highlights, editorial technical illustration, hand-drawn technical blog flowchart, clear, restrained, with an engineering sketch feel.

Composition: layered architecture diagram showing five nodes from left to right or top to bottom. 1. "CLI": terminal window and cursor, representing user input. 2. "Runtime": small console and gears, representing assembly of `ChatRequest`. 3. "Provider Contract": protocol card and bridge, highlighted in pale yellow, with the short label "Unified Contract." 4. "Provider Adapter": translator, small plug, or conversion arrow, representing translation from the unified request into a vendor API. 5. "Model API": cloud interface and model chip, representing a real LLM service.

Highlight: use pale yellow to emphasize Provider Contract and the arrow from Runtime to Provider Contract.

Background: very faint circuit traces, node links, and engineering sketch guide lines. They must stay subtle and not compete with the main subject.

Text: all visible labels must be in English only. Keep node labels short. At the bottom, leave room for the handwritten pull quote: "Provider translates capability; Core advances the task."

Overall feel: like a hand-drawn mechanism diagram in a technical blog, not a product UI and not a formal architecture diagram. Elements should be clear, hierarchy should be obvious, and arrow relationships should be unambiguous.

4. Negative Prompt
No photorealism, no 3D, no complex UI screenshots, no large blocks of code, no dense tiny text, no complex tables, no cyberpunk, no neon colors, no dark background, no colorful cartoon style, no complex shadows, no excessive decoration, no more than 6 main nodes, no background circuitry more prominent than the main subject, no non-English text, no non-English visible labels, no malformed words.
