1. Diagram type

Input-process-output, because Runtime produces stable fact events while TTY, JSONL, Workbench, CI, and Trace are projections.

2. Visual elements

Input: Runtime Events. Process: CLI Event Stream. Outputs: TTY Renderer, JSONL, Workbench, CI Log, Trace Store. Highlight CLI Event Stream and Trace Store.

3. Positive prompt

Create an in-article technical mechanism diagram about the stable output protocol of a Productized CLI.

Style: off-white paper, black hand-drawn ink linework, sparse pale-yellow highlights, technical blog sketch.

Composition: Runtime Events cards on the left feed into a thicker CLI Event Stream bus highlighted in pale yellow. The bus fans out to TTY Renderer terminal, JSONL braces document, Workbench timeline panel, CI Log build page, and Trace Store database highlighted. Caption area: "Terminal rendering is only one projection of events."

4. Negative prompt

No photorealism, no 3D, no complex UI, no dense text, no dark background, no neon, no excessive decoration, no long text.
