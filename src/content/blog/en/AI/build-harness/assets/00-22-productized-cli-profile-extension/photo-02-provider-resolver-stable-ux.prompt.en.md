1. Diagram type

Decision path, because multi-provider CLI chooses providers through profile and capability needs, then normalizes output into a single ModelEvent.

2. Visual elements

Model Request, Capability Need, Profile Preference, Provider Resolver, Provider A, Provider B fallback, ModelEvent, Core Runtime, Stable UX. Highlight Provider Resolver and ModelEvent.

3. Positive prompt

Create an in-article technical flow diagram about how multi-provider resolver keeps Agent CLI user experience stable.

Style: off-white paper, black hand-drawn ink linework, sparse pale-yellow highlights, technical blog sketch.

Composition: left-to-right decision path. Model Request card -> Capability Need puzzle -> Profile Preference note with sliders -> Provider Resolver signpost highlighted. Resolver branches to Provider A cloud and Provider B fallback cloud, then converges into one ModelEvent card highlighted, followed by Core Runtime and Stable UX terminal window. Caption area: "Providers may change; control semantics must not."

4. Negative prompt

No photorealism, no 3D, no complex UI, no dense text, no dark background, no neon, no excessive decoration, no long text.
