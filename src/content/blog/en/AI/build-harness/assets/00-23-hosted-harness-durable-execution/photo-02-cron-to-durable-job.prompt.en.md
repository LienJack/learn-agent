1. Diagram type

Circular loop, because Cron creates a recoverable job lifecycle rather than a one-shot command.

2. Visual elements

Loop nodes: Cron, Job, Queue, Worker, Session, Pause/Resume, Notify. Center: Agent Job card. Highlight Session and Notify.

3. Positive prompt

Create an in-article technical circular flow diagram about Cron evolving from timed command into durable job lifecycle.

Style: off-white paper, black hand-drawn ink lines, pale-yellow highlights, restrained engineering sketch.

Composition: central Agent Job card. Around it, clockwise loop: Cron calendar clock, Job task card, Queue line, Worker server, Session event ledger highlighted, Pause/Resume buttons, Notify message bubble highlighted. Arrows show Cron creates a job, worker advances it temporarily, session stores facts, task may pause, resume, or complete. Caption area: "Cron creates a task; it does not execute the world directly."

4. Negative prompt

No photorealism, no 3D, no UI screenshot, no dense text, no dark background, no neon, no excessive decoration, no long text.
