1. Figure Type
input-process-output diagram, because this image explains that the CLI only calls the runtime facade, while the runtime reads the registry, calls the provider, and emits stable events.

2. Visual Element List
6 main nodes: User Input, Runtime Facade, Registry, Provider Adapter, Event Bus, Runtime Output. Highlight Runtime Facade and Registry. Use icons for keyboard input, console facade, tool catalog card, cloud interface, logbook, and terminal output.

3. Positive Image Prompt
Draw an in-article technical explanation image with the theme: "Runtime Facade lets the CLI start a run without taking over provider and tool details."

Style: warm off-white paper background, black hand-drawn marker line art, slightly uneven line weight, a small amount of pale yellow highlighting, editorial technical illustration, hand-drawn flowchart for a technical blog, clear, restrained, with an engineering sketch feeling.

Composition: input-process-output diagram. On the left, draw a User Input keyboard with the short command "fix tests". An arrow enters the Runtime Facade console facade near the center-left, highlighted in pale yellow. Runtime Facade reads upward from the Registry tool-catalog card, which is also lightly highlighted in pale yellow; calls the Provider Adapter cloud interface to the right; writes downward to the Event Bus logbook; and outputs to a Runtime Output terminal on the far right, containing two small labels: text delta and tool intent. Use dashed lines to show that the CLI does not connect directly to Provider Adapter and does not directly execute anything inside Registry.

Background: very faint circuit lines, node links, and engineering sketch guide lines; they must not compete with the main subject. Use short English-only visible labels for every node. Leave a blank pull quote area at the bottom for text to be added later. Use English-only visible text. The overall feeling should be like a hand-drawn mechanism diagram in a technical blog, not a product UI and not a formal architecture diagram.

4. Negative Prompt
No photorealism, no 3D, no complex UI screenshots, no large blocks of code, no dense tiny text, no complex tables, no cyberpunk, no neon colors, no dark background, no colorful cartoon style, no complex shadows, no excessive decoration, no more than 6 main nodes, no background circuit lines more prominent than the main subject, no long text, no non-English visible text.
