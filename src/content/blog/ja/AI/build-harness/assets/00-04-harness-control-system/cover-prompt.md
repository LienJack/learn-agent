# cover: Harness の基礎定義：モデル外部の制御システム

Asset target: docs/ja/assets/00-04-harness-control-system/cover.png

imagegen を使って技術ブログのカバー画像を生成する。

## Prompt

「Harness はモデル外部の制御システムであり、Agent の各ステップを実行可能、制約可能、観察可能、復旧可能、検証可能にする」というテーマの技術ブログカバー画像を描く。

画風：アイボリーの紙背景、黒い手描きサインペンの線画、線幅は少し不均一、少量の淡い黄色ハイライト、editorial technical illustration、技術ブログ向けの手描き制御システムスケッチ、明快で抑制され、エンジニアリングスケッチ感がある。

構図：横長のブログカバー。中心に小さな Agent ノードを置き、その外側を多層の制御リングまたは制御パネルで包む：Permission、Sandbox、Trace、Replay、Eval。右側には Intent から Execution、さらに Observation へ進む制御された実行経路を置く。全体はエンジニアリング制御台のように見せるが、UI スクリーンショットにはしない。

ハイライト：淡い黄色で Harness 外側の制御リングと Permission ゲートを強調する。

背景：ごく淡い回路線、ノード接続線、エンジニアリングスケッチの補助線。主題を邪魔しない。

文字：短い英語ラベルのみを許可し、日本語の長いタイトルは生成しない。上部に、ページ側で記事タイトルを重ねられるきれいな余白を残す。

負向提示詞：写真写実にしない、3D にしない、複雑な UI スクリーンショットにしない、大量のコードを入れない、密集した小さな文字を入れない、複雑な表を入れない、サイバーパンクにしない、ネオン色にしない、暗い背景にしない、カラフルな漫画調にしない、複雑な影を入れない、装飾を増やしすぎない、主要ノードを 8 個より多くしない。
