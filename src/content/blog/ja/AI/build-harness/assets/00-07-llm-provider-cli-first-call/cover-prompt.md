# cover: LLM Provider 接続：CLI に最初のモデル呼び出しを通す

Asset target: docs/ja/assets/00-07-llm-provider-cli-first-call/cover.png

imagegen を使って技術ブログのカバー画像を生成する。

## Prompt

「実際の大規模モデルを CLI に接続するが、provider はモデル能力のアダプタ層に留め、Agent core を汚染しない」というテーマの技術ブログカバー画像を描く。

画風：アイボリーの紙背景、黒い手描きサインペンの線画、線幅は少し不均一、少量の淡い黄色ハイライト、editorial technical illustration、技術ブログ向けの手描きアーキテクチャスケッチ、明快で抑制され、エンジニアリングスケッチ感がある。

構図：横長のブログカバー。左側に簡潔な CLI 端末入力ボックス、中央に Provider Adapter 境界層、右側に Model API クラウドノードを置く。下方に保護された Core ボックスを置き、provider イベントは adapter を通ってのみ core contract に入ることを矢印で示す。全体として「最初のモデル呼び出し」と「境界分離」を強調する。

ハイライト：淡い黄色で Provider Adapter 境界と、CLI から Model へ向かう最初のストリーミング出力線を強調する。

背景：ごく淡い回路線、ノード接続線、エンジニアリングスケッチの補助線。主題を邪魔しない。

文字：短い英語ラベルのみを許可し、日本語の長いタイトルは生成しない。右上に、ページ側で記事タイトルを重ねられるきれいな余白を残す。

ネガティブプロンプト：写真写実にしない、3D にしない、複雑な UI スクリーンショットにしない、大量のコードを入れない、密集した小さな文字を入れない、複雑な表を入れない、サイバーパンクにしない、ネオン色にしない、暗い背景にしない、カラフルな漫画調にしない、複雑な影を入れない、装飾を増やしすぎない、主要ノードを 8 個より多くしない。
