1. 図解タイプ
横方向パイプライン。ここでは「モデルが意図を生成すること」と「システムが行動を実行すること」の異なる段階を区別するため。

2. 画面要素リスト
6 つのノード：モデル判断、tool call、プロトコル検証、権限チェック、ツール実行、observation 回填。tool call とツール実行の間の境界を強調する。縦の分界線でモデル側とシステム側を示す。

3. 正向画像提示詞
「tool call はツール実行ではない。モデルは意図だけを提示し、システムが実地へ落とす」というテーマの、記事内向け技術解説図を描く。

画風：アイボリーの紙背景、黒い手描きサインペンの線画、線幅は少し不均一、少量の淡い黄色ハイライト、editorial technical illustration、技術ブログ向けの手描きフローチャート、明快で抑制され、エンジニアリングスケッチ感がある。

構図：横方向パイプライン。画面中央に縦の点線境界を置き、左側を「モデル側」、右側を「システム側」とする。左から右へ 6 つのノードを描く：① 判断：モデルチップ。② tool call：小さな JSON カード、淡い黄色で強調する。③ プロトコル検証：ふるいアイコン。④ 権限：鍵と入退室ゲート。⑤ 実行：レンチと端末、淡い黄色で強調する。⑥ observation：ログ巻物がモデルコンテキストへ回流する。矢印が境界線を越える部分は太めに描き、責任境界の切り替わりを示す。

背景は淡い回路線とスケッチ補助線に保つ。ノードラベルは短く、実コードは描かない。下部に一文の空白名言スペースを残す。全体はエンジニアリング境界の解説図のようにする。

4. 負向提示詞
写真写実にしない、3D にしない、複雑な UI スクリーンショットにしない、大量のコードを入れない、密集した小さな文字を入れない、複雑な表を入れない、サイバーパンクにしない、ネオン色にしない、暗い背景にしない、カラフルな漫画調にしない、複雑な影を入れない、装飾を増やしすぎない、主要ノードを 6 個より多くしない、長い日本語文を生成しない。
