# cover: システム境界：ChatBot、Workflow、Agent、Harness の違い

Asset target: docs/ja/assets/00-03-chatbot-workflow-agent-harness/cover.png

imagegen を使って技術ブログのカバー画像を生成する。

## Prompt

「ChatBot、Workflow、Agent、Harness は能力ランクではなく、異なる不確実性とリスク境界に向き合うためのシステム選択である」というテーマの技術ブログカバー画像を描く。

画風：アイボリーの紙背景、黒い手描きサインペンの線画、線幅は少し不均一、少量の淡い黄色ハイライト、editorial technical illustration、技術ブログ向けの手描きフローチャート、明快で抑制され、エンジニアリングスケッチ感がある。

構図：横長のブログカバー。画面は決定境界マップ。横軸は Uncertainty、縦軸は Risk を表し、4 つの領域で ChatBot、Workflow、Agent、Harness を示す。ChatBot は左下、Workflow は左中、Agent は右中、Harness は右上に置く。各領域は簡単なアイコンと短いラベルのみで表し、密な座標は入れない。

ハイライト：淡い黄色で Agent と Harness の分界線を強調し、「動的意思決定」と「安定した托管」の違いを表現する。

背景：ごく淡い回路線、ノード接続線、エンジニアリングスケッチの補助線。主題を邪魔しない。

文字：短い英語ラベルのみを許可し、日本語の長いタイトルは生成しない。右側に、ページ側で記事タイトルを重ねられるきれいな余白を残す。

ネガティブプロンプト：写真写実にしない、3D にしない、複雑な UI スクリーンショットにしない、大量のコードを入れない、密集した小さな文字を入れない、複雑な表を入れない、サイバーパンクにしない、ネオン色にしない、暗い背景にしない、カラフルな漫画調にしない、複雑な影を入れない、装飾を増やしすぎない、主要ノードを 8 個より多くしない。
