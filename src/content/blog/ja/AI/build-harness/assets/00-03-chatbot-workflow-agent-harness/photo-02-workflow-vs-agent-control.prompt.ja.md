1. 図解タイプ
横方向パイプライン。ここでは Workflow と Agent の制御権の違いを比較し、左右分割で制御権の位置が異なることを表すため。

2. 画面要素リスト
左側 Workflow：固定ステップのレール。コードが次の一手を制御する。右側 Agent：観察、判断、行動の動的ループ。モデルが次の一手の選択に参加する。中央に縦線を置いて分割する。両側の「制御権」の位置を強調する。

3. 正向画像提示詞
「Workflow と Agent の違いは知能の高さではなく、制御権をどこに置くかである」というテーマの、記事内向け技術解説図を描く。

画風：アイボリーの紙背景、黒い手描きサインペンの線画、線幅は少し不均一、少量の淡い黄色ハイライト、editorial technical illustration、技術ブログ向けの手描きフローチャート、明快で抑制され、エンジニアリングスケッチ感がある。

構図：左右分割の対照図。左側の短いタイトルラベルは「Workflow」。固定レールを描き、コード取得、テスト実行、ログ送信の 3 つの小ノードを順につなぐ。上部に「コード制御」と書かれたコード用クリップボードを置き、淡い黄色で強調する。右側の短いタイトルラベルは「Agent」。観察、判断、行動、フィードバックの小ループを描く。ループ内の「判断」ノードはモデルチップで表し、淡い黄色で強調する。中央は縦の点線で分割し、左右の矢印スタイルを変える：左側は直線的、右側は分岐と回路を含む。

背景にはごく淡いエンジニアリング補助線を使う。長い日本語文は避ける。下部に空白の名言スペースを残す。全体は技術記事内の制御境界対照図のようにする。

4. 負向提示詞
写真写実にしない、3D にしない、複雑な UI スクリーンショットにしない、大量のコードを入れない、密集した小さな文字を入れない、複雑な表を入れない、サイバーパンクにしない、ネオン色にしない、暗い背景にしない、カラフルな漫画調にしない、複雑な影を入れない、装飾を増やしすぎない、主要ノードを 8 個より多くしない、長い日本語文を生成しない。
