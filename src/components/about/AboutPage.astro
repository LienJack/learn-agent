---
import type { AboutCopy } from '../../i18n/messages';
import AboutExperience from './AboutExperience';

interface Props {
	copy: AboutCopy;
}

const { copy } = Astro.props;
---

<AboutExperience copy={copy} client:load />
